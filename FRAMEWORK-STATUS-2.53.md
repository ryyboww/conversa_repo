# Framework Status — Convera Strategies 2.53.0

Source-level release audits and version-specific checks are included with this package.

A clean local Astro framework verification remains required before deployment:

```powershell
npm install
npm run check
npm run build
npm run dev
```

Do not reuse `node_modules`, `.astro`, or `dist` from an older release.
