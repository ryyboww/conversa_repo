# Framework Status — 2.50.0

Source-level repository, release, route, form, content, metadata, configuration, privacy, accessibility, and release-specific checks are run in this environment.

A full `npm ci`/Astro framework install could not be completed within the container execution window, so final `npm run check` and `npm run build` remain required on the clean local installation before deployment.
