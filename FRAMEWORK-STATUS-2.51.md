# Framework Status — 2.51.0

The 2.51 source-level release audits pass in the build environment. A complete Astro dependency install/check/build still requires the clean local installation sequence when dependency retrieval is unavailable or exceeds the execution window here.

Run locally:

```powershell
npm install
npm run check
npm run build
npm run dev
```
