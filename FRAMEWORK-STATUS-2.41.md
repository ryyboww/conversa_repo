# Framework Status — Convera Strategies 2.41.0

Source-level repository, route, form, content, metadata, configuration, privacy/exposure, and accessibility-source audits are run in this packaging environment.

The pinned Astro framework verification remains a local/CI requirement before deployment:

```powershell
npm install
npm run check
npm run build
```

Do not merge a prior `node_modules`, `.astro`, `dist`, or nested project tree into this release.
