# Framework Status — 2.43.0

Source-level repository, release, route, forms, content, metadata, configuration, privacy, accessibility, and release-specific audits are run before packaging. The distributed ZIP excludes `node_modules`, `.astro`, and `dist`; run `npm install`, `npm run check`, and `npm run build` from a clean local extraction for pinned-framework verification.
